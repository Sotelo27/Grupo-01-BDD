# Levantar la BDD
Para levantar la bdd con las tablas creadas pero vacías basta con correr ```docker compose up``` desde la raiz del repositorio.

Si se quieren llenar las tablas de datos de ejemplo, correr en lugar del comando anterior ```SEED=true docker compose up```.